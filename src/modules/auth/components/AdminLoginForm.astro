<form class="admin-login-form" data-admin-login-form>
	<p class="admin-login-alert" data-admin-login-message hidden aria-live="polite"></p>

	<label>
		<span>Correo electrónico</span>
		<input
			type="email"
			name="email"
			placeholder="tu-correo@dominio.com"
			autocomplete="email"
			required
		/>
	</label>

	<label>
		<span>Contraseña</span>
		<input
			type="password"
			name="password"
			placeholder="Ingresa tu contraseña"
			autocomplete="current-password"
			required
		/>
	</label>

	<button type="submit" data-admin-login-submit>
		<span data-admin-login-submit-label>Iniciar sesión</span>
	</button>
</form>

<style>
	.admin-login-form {
		display: grid;
		gap: 18px;
		margin-top: 28px;
	}

	.admin-login-form label {
		display: grid;
		gap: 8px;
	}

	.admin-login-form label span {
		color: var(--color-text-muted);
		font-size: 14px;
		font-weight: 800;
	}

	.admin-login-form input {
		height: 54px;
		border: 1px solid var(--color-border-muted);
		border-radius: 14px;
		background: var(--color-surface);
		color: var(--color-text);
		font: inherit;
		font-size: 16px;
		padding: 0 16px;
		outline: none;
		transition:
			border-color 180ms ease,
			box-shadow 180ms ease,
			background 180ms ease;
	}

	.admin-login-form input:focus {
		border-color: var(--color-brand);
		box-shadow: 0 0 0 3px color-mix(in srgb, var(--color-brand) 14%, transparent);
		background: var(--color-surface-alt);
	}

	.admin-login-form input::placeholder {
		color: var(--color-text-subtle);
	}

	.admin-login-form button {
		display: inline-flex;
		min-height: 56px;
		align-items: center;
		justify-content: center;
		border: 0;
		border-radius: 16px;
		background: linear-gradient(135deg, var(--color-brand) 0%, var(--color-brand-strong) 100%);
		color: var(--color-white);
		cursor: pointer;
		font: inherit;
		font-size: 16px;
		font-weight: 950;
		box-shadow: 0 18px 28px color-mix(in srgb, var(--color-brand) 24%, transparent);
		transition:
			transform 180ms ease,
			box-shadow 180ms ease,
			opacity 180ms ease;
	}

	.admin-login-form button:hover,
	.admin-login-form button:focus-visible {
		transform: translateY(-1px);
		box-shadow: 0 20px 34px color-mix(in srgb, var(--color-brand) 28%, transparent);
	}

	.admin-login-form button:disabled {
		cursor: wait;
		opacity: 0.82;
		transform: none;
	}

	.admin-login-alert {
		margin: 0;
		padding: 12px 14px;
		border-radius: 14px;
		font-size: 14px;
		font-weight: 700;
		line-height: 1.5;
	}

	.admin-login-alert.is-error {
		background: color-mix(in srgb, #d64545 12%, var(--color-surface));
		color: #922b2b;
	}

	.admin-login-alert.is-success {
		background: color-mix(in srgb, #1f8f5f 12%, var(--color-surface));
		color: #166344;
	}
</style>
