<div class="contacts-modal" data-contact-detail-modal hidden>
	<div class="contacts-modal__backdrop" data-contact-detail-close></div>
	<div class="contacts-modal__card" role="dialog" aria-modal="true">
		<div class="contacts-modal__header">
			<div>
				<p class="contacts-modal__eyebrow">Detalle del contacto</p>
				<h3 data-contact-detail-name></h3>
			</div>
			<button type="button" class="contacts-modal__close" data-contact-detail-close>×</button>
		</div>

		<div class="contacts-modal__grid">
			<div>
				<span class="contacts-modal__label">Teléfono</span>
				<p data-contact-detail-phone></p>
			</div>
			<div>
				<span class="contacts-modal__label">Correo</span>
				<p data-contact-detail-email></p>
			</div>
			<div>
				<span class="contacts-modal__label">Servicio</span>
				<p data-contact-detail-service></p>
			</div>
			<div>
				<span class="contacts-modal__label">Fecha</span>
				<p data-contact-detail-date></p>
			</div>
		</div>

		<div class="contacts-modal__message">
			<span class="contacts-modal__label">Mensaje</span>
			<p data-contact-detail-message></p>
		</div>
	</div>
</div>

<style>
	.contacts-modal[hidden] {
		display: none;
	}

	.contacts-modal {
		position: fixed;
		inset: 0;
		z-index: 80;
		display: grid;
		place-items: center;
		padding: 24px;
	}

	.contacts-modal__backdrop {
		position: absolute;
		inset: 0;
		background: rgb(15 23 42 / 0.48);
		backdrop-filter: blur(6px);
	}

	.contacts-modal__card {
		position: relative;
		z-index: 1;
		width: min(100%, 760px);
		padding: 28px;
		border: 1px solid var(--color-border-soft);
		border-radius: 24px;
		background: var(--color-surface);
		box-shadow: 0 26px 60px rgb(15 23 42 / 0.18);
	}

	.contacts-modal__header {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 16px;
	}

	.contacts-modal__eyebrow {
		margin: 0 0 8px;
		color: var(--color-brand);
		font-size: 12px;
		font-weight: 900;
		letter-spacing: 0.12em;
		text-transform: uppercase;
	}

	.contacts-modal__header h3 {
		margin: 0;
		color: var(--color-text);
		font-size: 28px;
		font-weight: 950;
		line-height: 1.05;
	}

	.contacts-modal__close {
		width: 42px;
		height: 42px;
		border: 1px solid var(--color-border-muted);
		border-radius: 12px;
		background: var(--color-surface);
		color: var(--color-text);
		cursor: pointer;
		font-size: 24px;
		line-height: 1;
	}

	.contacts-modal__grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 18px;
		margin-top: 24px;
	}

	.contacts-modal__grid > div,
	.contacts-modal__message {
		padding: 22px;
		border: 1px solid var(--color-border-soft);
		border-radius: 18px;
		background: color-mix(in srgb, var(--color-surface-alt) 78%, var(--color-surface));
	}

	.contacts-modal__label {
		display: block;
		color: var(--color-text-subtle);
		font-size: 12px;
		font-weight: 900;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.contacts-modal__grid p,
	.contacts-modal__message p {
		margin: 8px 0 0;
		color: var(--color-text);
		font-size: 16px;
		line-height: 1.7;
		white-space: pre-wrap;
	}

	.contacts-modal__message {
		margin-top: 18px;
	}

	@media (max-width: 640px) {
		.contacts-modal {
			padding: 14px;
		}

		.contacts-modal__card {
			padding: 22px 18px;
			border-radius: 20px;
		}

		.contacts-modal__grid {
			grid-template-columns: 1fr;
		}
	}
</style>
